spring.application.name=Estate Application Back-end
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

spring.datasource.driver-class-name=${DATABASE_DRIVER}

spring.jpa.hibernate.ddl-auto=${DDL_AUTO}
spring.jpa.show-sql=${SHOW_SQL}
spring.jpa.open-in-view=${OPEN_IN_VIEW}

logging.level.org.springframework=${LOGGING_LEVEL_SPRING}
logging.level.com.openclassrooms.estate_back_end=${LOGGING_LEVEL_APP}